import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Office Suit"),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Start editing the HomeScreen'),
      ),
    );
  }
}
